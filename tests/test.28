#!/bin/bash

source common

DESC="generate rmsh, hard links"

rm -f rmsh.sh

$DUPD_CMD scan --path `pwd`/files2 -q
checkrv $?

$DUPD_CMD rmsh --hardlink > rmsh.sh

if [ -e rmsh.sh ]; then
    echo "OK ${DESC}"
else
    echo "FAIL ${DESC}"
    exit 1
fi

rm -f rmsh.sh

tdone
