#!/bin/bash

source common

DESC="relative --file"

$DUPD_CMD scan --path `pwd`/files2 -q
checkrv $?

$DUPD_CMD file --file files2/lonely/unique_size --cut `pwd`/files2/ > report
checkrv $?

diff report output.44
checkrv $?

tdone
