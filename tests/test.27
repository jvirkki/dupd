#!/bin/bash

source common

DESC="generate rmsh, soft links"

rm -f rmsh.sh

$DUPD_CMD scan --path `pwd`/files2 -q
checkrv $?

$DUPD_CMD rmsh --link > rmsh.sh

if [ -e rmsh.sh ]; then
    echo "OK ${DESC}"
else
    echo "FAIL ${DESC}"
    exit 1
fi

rm -f rmsh.sh

tdone
