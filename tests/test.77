#!/usr/bin/env bash

source common

DESC="scan(files)"
$DUPD_CMD scan --path `pwd`/files -q --hdd
checkrv $?

DESC="csv report"
$DUPD_CMD report --format csv --cut `pwd`/files/ > nreport
checkrv $?

check_nreport output.77

tdone
