#!/bin/bash

source common

DESC="scan with --minsize"

$DUPD_CMD scan --path `pwd`/files --minsize 10000 -q
checkrv $?

$DUPD_CMD report --cut `pwd`/files/ | grep -v "Duplicate report from database" > nreport
checkrv $?

sort nreport > report
diff report output.41
checkrv $?

tdone
