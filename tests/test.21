#!/bin/bash

source common

DESC="scan with --intblocks 5"

$DUPD_CMD scan --path `pwd`/files -q --intblocks 5
checkrv $?

$DUPD_CMD ls --cut `pwd`/files/ --path `pwd`/files/ > nreport
checkrv $?

sort nreport > report
diff report output.08
checkrv $?

tdone
